# Cloudflare Workers API環境変数サンプル
# このファイルを .dev.vars にコピーして使用してください (api/ディレクトリ内)

# Google OAuth2認証設定
# Google Cloud Console から取得
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT署名用シークレット
# ランダムな文字列を生成してください
# 例: openssl rand -base64 32
JWT_SECRET=your-random-jwt-secret-key

# Google Sheets ID
# データが格納されているGoogle SpreadsheetsのID
# URLの /d/ と /edit の間の文字列
# 例: https://docs.google.com/spreadsheets/d/[この部分]/edit
GOOGLE_SHEETS_ID=your-spreadsheet-id

# サービスアカウント認証情報 (オプション)
# Google Cloud Console から取得したJSONキーファイルの内容
SERVICE_ACCOUNT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com
SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ドメイン制限設定 (wrangler.tomlで設定)
# ALLOWED_DOMAIN=medicmedia.com

# KV Namespace ID (wrangler.tomlで設定)
# 以下のコマンドで作成後、IDを取得:
# wrangler kv:namespace create "EXAM_CACHE"
# wrangler kv:namespace create "IMAGE_CACHE"
# wrangler kv:namespace create "SESSION_STORE"